#include <stdio.h>
int main()
{
    int n,k;
    scanf("%d",&n);
    for(int i=1;i<n;i++)
    {
        if(i*i==n)
        {
        printf("true");
        goto end;
        }
    }
    printf("False");
    end:
}
